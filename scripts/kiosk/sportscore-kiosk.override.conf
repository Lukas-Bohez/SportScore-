[Service]
# Replace ExecStart so the systemd instance opens the live QR screen directly
# Use the working Pi host and a hash route to ensure the SPA loads the bigscreen QR view on boot
ExecStart=
ExecStart=/home/%i/.local/bin/start-kiosk.sh http://192.168.123.241/#/bigscreen/qrscreen
